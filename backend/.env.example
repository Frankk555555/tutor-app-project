# Server Port (Render will override this, local defaults to 5000)
PORT=5000

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tutor_db
DB_PORT=3307

# JWT Secret Key
JWT_SECRET=your_super_secret_key_that_is_long_and_random
